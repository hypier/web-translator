# AINetwork

>[AI Network](https://www.ainetwork.ai/build-on-ain) 是一个层 1 区块链，旨在支持大规模 AI 模型，利用去中心化的 GPU 网络，由 [$AIN token](https://www.ainetwork.ai/token) 提供支持，丰富 AI 驱动的 `NFTs` (`AINFTs`)。

## 安装与设置

您需要安装 `ain-py` python 包。

```bash
pip install ain-py
```
您需要将 `AIN_BLOCKCHAIN_ACCOUNT_PRIVATE_KEY` 环境变量设置为您的 AIN 区块链账户私钥。

## 工具包

查看 [使用示例](/docs/integrations/toolkits/ainetwork)。

```python
from langchain_community.agent_toolkits.ainetwork.toolkit import AINetworkToolkit
```