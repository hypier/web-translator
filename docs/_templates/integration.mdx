[comment: 请在这里提供一个参考示例 "docs/integrations/arxiv.md"]::  
[comment: 使用此模板在 "docs/integrations/" 中创建一个新的 .md 文件]::

# Title_REPLACE_ME

[comment: 只允许一个标题/H1!]::

>
[comment: 描述：阅读此描述后，读者应该决定此集成是否值得尝试/继续阅读，或者]::
[comment: 转到阅读下一个集成文档。]::
[comment: 描述应包含一个链接，以便继续阅读来源。]::

## 安装与设置

[comment: 安装与设置：所有必要的额外包安装和设置，例如 Tokens 等]::

```bash
pip install package_name_REPLACE_ME
```

[comment: 或者这段文本:]::

没有任何特殊的设置要求。

[comment: 接下来的 H2/## 部分包含集成模块的名称，例如 "LLM"、"文本嵌入模型" 等]::
[comment: 请参见 "index.html" 页面中的 "模块"]::
[comment: 每个 H2 部分应包含指向示例和导入集成类的 Python 代码的链接]::
[comment: 以下是几个示例部分。删除所有不必要的部分。添加所有未提供的必要部分。]::

## LLM

请参阅[使用示例](/docs/integrations/llms/INCLUDE_REAL_NAME)。

```python
from langchain_community.llms import integration_class_REPLACE_ME
```

## 文本嵌入模型

请参见[使用示例](/docs/integrations/text_embedding/INCLUDE_REAL_NAME)。

```python
from langchain_community.embeddings import integration_class_REPLACE_ME
```

## 聊天模型

查看[使用示例](/docs/integrations/chat/INCLUDE_REAL_NAME).

```python
from langchain_community.chat_models import integration_class_REPLACE_ME
```

## 文档加载器

查看[使用示例](/docs/integrations/document_loaders/INCLUDE_REAL_NAME)。

```python
from langchain_community.document_loaders import integration_class_REPLACE_ME
```